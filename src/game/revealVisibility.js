// Keep the historical import path for views and Train hand-offs while the
// canonical implementation lives at the reducer boundary that must enforce it.
export { isOptionRevealed } from './gameState.js'
